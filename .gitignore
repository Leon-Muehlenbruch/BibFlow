# macOS
.DS_Store
.AppleDouble
.LSOverride

# Obsidian workspace state (changes on every session, creates noise)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/workspace.json.bak

# Obsidian cache
.obsidian/cache

# Trash folder
.trash/

# Python venv for the Templater Scripts helpers (set up once per machine,
# see "9 Orga/Templater Scripts/README.md"). Machine-specific — never commit.
9 Orga/Templater Scripts/.venv/
**/__pycache__/

# Claude Code per-machine permission overrides (keep personal grants local)
.claude/settings.local.json
